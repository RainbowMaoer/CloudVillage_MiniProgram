<!--pages/detail-video/index.wxml-->
<video
  class="video"
  src="{{mvURLInfo.url}}"
  referrer-policy="origin"
  autoplay
  loop
>
</video>

<!-- 下面模拟数据 -->
<block wx:for="{{100}}" wx:key="*this">
  <view>列表数据{{item}}</view>
</block>
